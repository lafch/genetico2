/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package net.uch.solicitud.hibernateSpringDao;

import java.util.List;
import net.uch.mapping.AdAreaInstitucion;

/**
 *
 * @author biblioteca
 */
public interface AdAreaInstitucionDAO {

    void attachClean(AdAreaInstitucion instance);

    void attachDirty(AdAreaInstitucion instance);

    void delete(AdAreaInstitucion persistentInstance);

    List findAll();

    List findByExample(AdAreaInstitucion instance);

    AdAreaInstitucion findById(Integer id);

    List findByProperty(String propertyName, Object value);

    AdAreaInstitucion merge(AdAreaInstitucion detachedInstance);

    void save(AdAreaInstitucion transientInstance);
    
}
